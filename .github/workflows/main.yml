name: Scheduled build
on:
  schedule:
    - cron: '0 7 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger our build webhook on Vercel
        env:
          URI: ${{ secrets.VERCEL_CRON_BUILD_HOOK }}
        run: curl -s -X POST "$URI"
